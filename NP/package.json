{
  "name": "hoho-test-np",
  "version": "1.0.5-alpha.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo \"build successful\" ",
    "test": "echo \"Error: no test specified\"",
    "publish": "npm i  && npm run build",
    "prepublish": "npm i  && npm run build && npm test",
    "deploy": "np"
  },
  "author": "hohozhu",
  "license": "ISC",
  "maintainers": [
    "ones"
  ],
  "publishConfig": {
    "registry": "https://npm2.myones.net/",
    "access": "public"
  },
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "branchsite": "^4.0.3"
  },
  "dependencies": {
    "np": "^7.6.3"
  }
}
